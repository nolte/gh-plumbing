name: Dependency Review

on:
  pull_request:

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nolte/gh-plumbing/.github/workflows/reusable-dependency-review.yaml@fix/388-pin-third-party-actions
